    <div class="bg-light text-center pt-5" style="height: 80vh;">
        <div class="container">
            <h1 class="display-1 pt-5 font-weight-bold text-uppercase">{{{code}}}</h1>
            <br>
            <h3 class="font-weight-light text-secondary">{{{message}}}</h3>
            <a href="{{redirect}}" class="button-ef mt-3">Regresar a la tienda</a>
        </div>
    </div>